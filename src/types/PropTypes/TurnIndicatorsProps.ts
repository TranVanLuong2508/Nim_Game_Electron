import type { GameState } from "@/types/gameState"
import type { GameSettings } from "@/types/settings"

interface TurnIndicatorsProps {
    gameState: GameState
    settings: GameSettings
}

export type { TurnIndicatorsProps }